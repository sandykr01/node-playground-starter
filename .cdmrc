tabs: ['index.js', 'index.html']
playground-view: terminal-editor-browser

# terminals
terminals:
    ['touch index.js && clear && echo Are you ready pal!']
# other config
run-button: clear && node index.js
